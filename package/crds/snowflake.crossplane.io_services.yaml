---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: services.snowflake.crossplane.io
spec:
  group: snowflake.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - snowflake
    kind: Service
    listKind: ServiceList
    plural: services
    singular: service
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Service is the Schema for the Services API. Resource used to
          manage services. For more information, check services documentation https://docs.snowflake.com/en/sql-reference/sql/create-service.
          A long-running service is like a web service that does not end automatically.
          After you create a service, Snowflake manages the running service. For example,
          if a service container stops, for whatever reason, Snowflake restarts that
          container so the service runs uninterrupted. See Working with services https://docs.snowflake.com/en/developer-guide/snowpark-container-services/working-with-services
          developer guide for more details.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceSpec defines the desired state of Service
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  autoResume:
                    description: |-
                      uses special value that cannot be set in the configuration manually (default)) Specifies whether to automatically resume a service. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                      (Default: fallback to Snowflake default - uses special value that cannot be set in the configuration manually (`default`)) Specifies whether to automatically resume a service. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                    type: string
                  autoSuspendSecs:
                    description: |-
                      uses special value that cannot be set in the configuration manually (-1)) Specifies the number of seconds of inactivity (service is idle) after which Snowflake automatically suspends the service.
                      (Default: fallback to Snowflake default - uses special value that cannot be set in the configuration manually (`-1`)) Specifies the number of seconds of inactivity (service is idle) after which Snowflake automatically suspends the service.
                    type: number
                  comment:
                    description: |-
                      (String) Specifies a comment for the service.
                      Specifies a comment for the service.
                    type: string
                  computePool:
                    description: |-
                      case characters are not supported. This limitation in the provider follows the limitation in Snowflake (see docs). Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      Specifies the name of the compute pool in your account on which to run the service. Identifiers with special or lower-case characters are not supported. This limitation in the provider follows the limitation in Snowflake (see [docs](https://docs.snowflake.com/en/sql-reference/sql/create-compute-pool)). Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the service. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the service. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  externalAccessIntegrations:
                    description: |-
                      (Set of String) Specifies the names of the external access integrations that allow your service to access external sites.
                      Specifies the names of the external access integrations that allow your service to access external sites.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fromSpecification:
                    description: |-
                      (Block List, Max: 1) Specifies the service specification to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See service specification for more information. (see below for nested schema)
                      Specifies the service specification to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See [service specification](https://docs.snowflake.com/en/developer-guide/snowpark-container-services/specification-reference#general-guidelines) for more information.
                    items:
                      properties:
                        file:
                          description: |-
                            (String) The file name of the service specification. Example: spec.yaml.
                            The file name of the service specification. Example: `spec.yaml`.
                          type: string
                        path:
                          description: |-
                            (String) The path to the service specification file on the given stage. When the path is specified, the / character is automatically added as a path prefix. Example: path/to/spec.
                            The path to the service specification file on the given stage. When the path is specified, the `/` character is automatically added as a path prefix. Example: `path/to/spec`.
                          type: string
                        stage:
                          description: |-
                            (String) The fully qualified name of the stage containing the service specification file. At symbol (@) is added automatically. Example: "\"<db_name>\".\"<schema_name>\".\"<stage_name>\"". For more information about this resource, see docs.
                            The fully qualified name of the stage containing the service specification file. At symbol (`@`) is added automatically. Example: `"\"<db_name>\".\"<schema_name>\".\"<stage_name>\""`. For more information about this resource, see [docs](./stage).
                          type: string
                        text:
                          description: |-
                            (String) The embedded text of the service specification.
                            The embedded text of the service specification.
                          type: string
                      type: object
                    type: array
                  fromSpecificationTemplate:
                    description: |-
                      (Block List, Max: 1) Specifies the service specification template to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See service specification for more information. (see below for nested schema)
                      Specifies the service specification template to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See [service specification](https://docs.snowflake.com/en/developer-guide/snowpark-container-services/specification-reference#general-guidelines) for more information.
                    items:
                      properties:
                        file:
                          description: |-
                            (String) The file name of the service specification. Example: spec.yaml.
                            The file name of the service specification template. Example: `spec.yaml`.
                          type: string
                        path:
                          description: |-
                            (String) The path to the service specification file on the given stage. When the path is specified, the / character is automatically added as a path prefix. Example: path/to/spec.
                            The path to the service specification template file on the given stage. When the path is specified, the `/` character is automatically added as a path prefix. Example: `path/to/spec`.
                          type: string
                        stage:
                          description: |-
                            (String) The fully qualified name of the stage containing the service specification file. At symbol (@) is added automatically. Example: "\"<db_name>\".\"<schema_name>\".\"<stage_name>\"". For more information about this resource, see docs.
                            The fully qualified name of the stage containing the service specification template file. At symbol (`@`) is added automatically. Example: `"\"<db_name>\".\"<schema_name>\".\"<stage_name>\""`. For more information about this resource, see [docs](./stage).
                          type: string
                        text:
                          description: |-
                            (String) The embedded text of the service specification.
                            The embedded text of the service specification template.
                          type: string
                        using:
                          description: |-
                            (Block List, Min: 1) List of the specified template variables and the values of those variables. (see below for nested schema)
                            List of the specified template variables and the values of those variables.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The name of the template variable. The provider wraps it in double quotes by default, so be aware of that while referencing the argument in the spec definition.
                                  The name of the template variable. The provider wraps it in double quotes by default, so be aware of that while referencing the argument in the spec definition.
                                type: string
                              value:
                                description: |-
                                  (String) The value to assign to the variable in the template. The provider wraps it in $$ by default, so be aware of that while referencing the argument in the spec definition. The value must either be alphanumeric or valid JSON.
                                  The value to assign to the variable in the template. The provider wraps it in `$$` by default, so be aware of that while referencing the argument in the spec definition. The value must either be alphanumeric or valid JSON.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  maxInstances:
                    description: |-
                      (Number) Specifies the maximum number of service instances to run.
                      Specifies the maximum number of service instances to run.
                    type: number
                  minInstances:
                    description: |-
                      (Number) Specifies the minimum number of service instances to run.
                      Specifies the minimum number of service instances to run.
                    type: number
                  minReadyInstances:
                    description: |-
                      (Number) Indicates the minimum service instances that must be ready for Snowflake to consider the service is ready to process requests.
                      Indicates the minimum service instances that must be ready for Snowflake to consider the service is ready to process requests.
                    type: number
                  queryWarehouse:
                    description: |-
                      (String) Warehouse to use if a service container connects to Snowflake to execute a query but does not explicitly specify a warehouse to use. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      Warehouse to use if a service container connects to Snowflake to execute a query but does not explicitly specify a warehouse to use. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  schema:
                    description: |-
                      (String) The schema in which to create the service. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the service. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoResume:
                    description: |-
                      uses special value that cannot be set in the configuration manually (default)) Specifies whether to automatically resume a service. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                      (Default: fallback to Snowflake default - uses special value that cannot be set in the configuration manually (`default`)) Specifies whether to automatically resume a service. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                    type: string
                  autoSuspendSecs:
                    description: |-
                      uses special value that cannot be set in the configuration manually (-1)) Specifies the number of seconds of inactivity (service is idle) after which Snowflake automatically suspends the service.
                      (Default: fallback to Snowflake default - uses special value that cannot be set in the configuration manually (`-1`)) Specifies the number of seconds of inactivity (service is idle) after which Snowflake automatically suspends the service.
                    type: number
                  comment:
                    description: |-
                      (String) Specifies a comment for the service.
                      Specifies a comment for the service.
                    type: string
                  computePool:
                    description: |-
                      case characters are not supported. This limitation in the provider follows the limitation in Snowflake (see docs). Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      Specifies the name of the compute pool in your account on which to run the service. Identifiers with special or lower-case characters are not supported. This limitation in the provider follows the limitation in Snowflake (see [docs](https://docs.snowflake.com/en/sql-reference/sql/create-compute-pool)). Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the service. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the service. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  externalAccessIntegrations:
                    description: |-
                      (Set of String) Specifies the names of the external access integrations that allow your service to access external sites.
                      Specifies the names of the external access integrations that allow your service to access external sites.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fromSpecification:
                    description: |-
                      (Block List, Max: 1) Specifies the service specification to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See service specification for more information. (see below for nested schema)
                      Specifies the service specification to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See [service specification](https://docs.snowflake.com/en/developer-guide/snowpark-container-services/specification-reference#general-guidelines) for more information.
                    items:
                      properties:
                        file:
                          description: |-
                            (String) The file name of the service specification. Example: spec.yaml.
                            The file name of the service specification. Example: `spec.yaml`.
                          type: string
                        path:
                          description: |-
                            (String) The path to the service specification file on the given stage. When the path is specified, the / character is automatically added as a path prefix. Example: path/to/spec.
                            The path to the service specification file on the given stage. When the path is specified, the `/` character is automatically added as a path prefix. Example: `path/to/spec`.
                          type: string
                        stage:
                          description: |-
                            (String) The fully qualified name of the stage containing the service specification file. At symbol (@) is added automatically. Example: "\"<db_name>\".\"<schema_name>\".\"<stage_name>\"". For more information about this resource, see docs.
                            The fully qualified name of the stage containing the service specification file. At symbol (`@`) is added automatically. Example: `"\"<db_name>\".\"<schema_name>\".\"<stage_name>\""`. For more information about this resource, see [docs](./stage).
                          type: string
                        text:
                          description: |-
                            (String) The embedded text of the service specification.
                            The embedded text of the service specification.
                          type: string
                      type: object
                    type: array
                  fromSpecificationTemplate:
                    description: |-
                      (Block List, Max: 1) Specifies the service specification template to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See service specification for more information. (see below for nested schema)
                      Specifies the service specification template to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See [service specification](https://docs.snowflake.com/en/developer-guide/snowpark-container-services/specification-reference#general-guidelines) for more information.
                    items:
                      properties:
                        file:
                          description: |-
                            (String) The file name of the service specification. Example: spec.yaml.
                            The file name of the service specification template. Example: `spec.yaml`.
                          type: string
                        path:
                          description: |-
                            (String) The path to the service specification file on the given stage. When the path is specified, the / character is automatically added as a path prefix. Example: path/to/spec.
                            The path to the service specification template file on the given stage. When the path is specified, the `/` character is automatically added as a path prefix. Example: `path/to/spec`.
                          type: string
                        stage:
                          description: |-
                            (String) The fully qualified name of the stage containing the service specification file. At symbol (@) is added automatically. Example: "\"<db_name>\".\"<schema_name>\".\"<stage_name>\"". For more information about this resource, see docs.
                            The fully qualified name of the stage containing the service specification template file. At symbol (`@`) is added automatically. Example: `"\"<db_name>\".\"<schema_name>\".\"<stage_name>\""`. For more information about this resource, see [docs](./stage).
                          type: string
                        text:
                          description: |-
                            (String) The embedded text of the service specification.
                            The embedded text of the service specification template.
                          type: string
                        using:
                          description: |-
                            (Block List, Min: 1) List of the specified template variables and the values of those variables. (see below for nested schema)
                            List of the specified template variables and the values of those variables.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The name of the template variable. The provider wraps it in double quotes by default, so be aware of that while referencing the argument in the spec definition.
                                  The name of the template variable. The provider wraps it in double quotes by default, so be aware of that while referencing the argument in the spec definition.
                                type: string
                              value:
                                description: |-
                                  (String) The value to assign to the variable in the template. The provider wraps it in $$ by default, so be aware of that while referencing the argument in the spec definition. The value must either be alphanumeric or valid JSON.
                                  The value to assign to the variable in the template. The provider wraps it in `$$` by default, so be aware of that while referencing the argument in the spec definition. The value must either be alphanumeric or valid JSON.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  maxInstances:
                    description: |-
                      (Number) Specifies the maximum number of service instances to run.
                      Specifies the maximum number of service instances to run.
                    type: number
                  minInstances:
                    description: |-
                      (Number) Specifies the minimum number of service instances to run.
                      Specifies the minimum number of service instances to run.
                    type: number
                  minReadyInstances:
                    description: |-
                      (Number) Indicates the minimum service instances that must be ready for Snowflake to consider the service is ready to process requests.
                      Indicates the minimum service instances that must be ready for Snowflake to consider the service is ready to process requests.
                    type: number
                  queryWarehouse:
                    description: |-
                      (String) Warehouse to use if a service container connects to Snowflake to execute a query but does not explicitly specify a warehouse to use. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      Warehouse to use if a service container connects to Snowflake to execute a query but does not explicitly specify a warehouse to use. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  schema:
                    description: |-
                      (String) The schema in which to create the service. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the service. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.computePool is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.computePool)
                || (has(self.initProvider) && has(self.initProvider.computePool))'
            - message: spec.forProvider.database is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.database)
                || (has(self.initProvider) && has(self.initProvider.database))'
            - message: spec.forProvider.schema is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.schema)
                || (has(self.initProvider) && has(self.initProvider.schema))'
          status:
            description: ServiceStatus defines the observed state of Service.
            properties:
              atProvider:
                properties:
                  autoResume:
                    description: |-
                      uses special value that cannot be set in the configuration manually (default)) Specifies whether to automatically resume a service. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                      (Default: fallback to Snowflake default - uses special value that cannot be set in the configuration manually (`default`)) Specifies whether to automatically resume a service. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                    type: string
                  autoSuspendSecs:
                    description: |-
                      uses special value that cannot be set in the configuration manually (-1)) Specifies the number of seconds of inactivity (service is idle) after which Snowflake automatically suspends the service.
                      (Default: fallback to Snowflake default - uses special value that cannot be set in the configuration manually (`-1`)) Specifies the number of seconds of inactivity (service is idle) after which Snowflake automatically suspends the service.
                    type: number
                  comment:
                    description: |-
                      (String) Specifies a comment for the service.
                      Specifies a comment for the service.
                    type: string
                  computePool:
                    description: |-
                      case characters are not supported. This limitation in the provider follows the limitation in Snowflake (see docs). Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      Specifies the name of the compute pool in your account on which to run the service. Identifiers with special or lower-case characters are not supported. This limitation in the provider follows the limitation in Snowflake (see [docs](https://docs.snowflake.com/en/sql-reference/sql/create-compute-pool)). Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the service. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the service. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  describeOutput:
                    description: |-
                      (List of Object) Outputs the result of DESCRIBE SERVICE for the given service. (see below for nested schema)
                      Outputs the result of `DESCRIBE SERVICE` for the given service.
                    items:
                      properties:
                        autoResume:
                          description: 'uses special value that cannot be set in the
                            configuration manually (default)) Specifies whether to
                            automatically resume a service. Available options are:
                            "true" or "false". When the value is not set in the configuration
                            the provider will put "default" there which means to use
                            the Snowflake default for this value.'
                          type: boolean
                        autoSuspendSecs:
                          description: uses special value that cannot be set in the
                            configuration manually (-1)) Specifies the number of seconds
                            of inactivity (service is idle) after which Snowflake
                            automatically suspends the service.
                          type: number
                        comment:
                          description: (String) Specifies a comment for the service.
                          type: string
                        computePool:
                          description: 'case characters are not supported. This limitation
                            in the provider follows the limitation in Snowflake (see
                            docs). Due to technical limitations (read more here),
                            avoid using the following characters: |, ., ".'
                          type: string
                        createdOn:
                          description: (String)
                          type: string
                        currentInstances:
                          description: (Number)
                          type: number
                        databaseName:
                          description: (String)
                          type: string
                        dnsName:
                          description: (String)
                          type: string
                        externalAccessIntegrations:
                          description: (Set of String) Specifies the names of the
                            external access integrations that allow your service to
                            access external sites.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        isAsyncJob:
                          description: (Boolean)
                          type: boolean
                        isJob:
                          description: (Boolean)
                          type: boolean
                        isUpgrading:
                          description: (Boolean)
                          type: boolean
                        managingObjectDomain:
                          description: (String)
                          type: string
                        managingObjectName:
                          description: (String)
                          type: string
                        maxInstances:
                          description: (Number) Specifies the maximum number of service
                            instances to run.
                          type: number
                        minInstances:
                          description: (Number) Specifies the minimum number of service
                            instances to run.
                          type: number
                        minReadyInstances:
                          description: (Number) Indicates the minimum service instances
                            that must be ready for Snowflake to consider the service
                            is ready to process requests.
                          type: number
                        name:
                          description: '(String) Specifies the identifier for the
                            service; must be unique for the schema in which the service
                            is created. Due to technical limitations (read more here),
                            avoid using the following characters: |, ., ".'
                          type: string
                        owner:
                          description: (String)
                          type: string
                        ownerRoleType:
                          description: (String)
                          type: string
                        queryWarehouse:
                          description: '(String) Warehouse to use if a service container
                            connects to Snowflake to execute a query but does not
                            explicitly specify a warehouse to use. Due to technical
                            limitations (read more here), avoid using the following
                            characters: |, ., ".'
                          type: string
                        resumedOn:
                          description: (String)
                          type: string
                        schemaName:
                          description: (String)
                          type: string
                        spec:
                          description: (String)
                          type: string
                        specDigest:
                          description: (String)
                          type: string
                        status:
                          description: (String)
                          type: string
                        suspendedOn:
                          description: (String)
                          type: string
                        targetInstances:
                          description: (Number)
                          type: number
                        updatedOn:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  externalAccessIntegrations:
                    description: |-
                      (Set of String) Specifies the names of the external access integrations that allow your service to access external sites.
                      Specifies the names of the external access integrations that allow your service to access external sites.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fromSpecification:
                    description: |-
                      (Block List, Max: 1) Specifies the service specification to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See service specification for more information. (see below for nested schema)
                      Specifies the service specification to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See [service specification](https://docs.snowflake.com/en/developer-guide/snowpark-container-services/specification-reference#general-guidelines) for more information.
                    items:
                      properties:
                        file:
                          description: |-
                            (String) The file name of the service specification. Example: spec.yaml.
                            The file name of the service specification. Example: `spec.yaml`.
                          type: string
                        path:
                          description: |-
                            (String) The path to the service specification file on the given stage. When the path is specified, the / character is automatically added as a path prefix. Example: path/to/spec.
                            The path to the service specification file on the given stage. When the path is specified, the `/` character is automatically added as a path prefix. Example: `path/to/spec`.
                          type: string
                        stage:
                          description: |-
                            (String) The fully qualified name of the stage containing the service specification file. At symbol (@) is added automatically. Example: "\"<db_name>\".\"<schema_name>\".\"<stage_name>\"". For more information about this resource, see docs.
                            The fully qualified name of the stage containing the service specification file. At symbol (`@`) is added automatically. Example: `"\"<db_name>\".\"<schema_name>\".\"<stage_name>\""`. For more information about this resource, see [docs](./stage).
                          type: string
                        text:
                          description: |-
                            (String) The embedded text of the service specification.
                            The embedded text of the service specification.
                          type: string
                      type: object
                    type: array
                  fromSpecificationTemplate:
                    description: |-
                      (Block List, Max: 1) Specifies the service specification template to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See service specification for more information. (see below for nested schema)
                      Specifies the service specification template to use for the service. Note that external changes on this field and nested fields are not detected. Use correctly formatted YAML files. Watch out for the space/tabs indentation. See [service specification](https://docs.snowflake.com/en/developer-guide/snowpark-container-services/specification-reference#general-guidelines) for more information.
                    items:
                      properties:
                        file:
                          description: |-
                            (String) The file name of the service specification. Example: spec.yaml.
                            The file name of the service specification template. Example: `spec.yaml`.
                          type: string
                        path:
                          description: |-
                            (String) The path to the service specification file on the given stage. When the path is specified, the / character is automatically added as a path prefix. Example: path/to/spec.
                            The path to the service specification template file on the given stage. When the path is specified, the `/` character is automatically added as a path prefix. Example: `path/to/spec`.
                          type: string
                        stage:
                          description: |-
                            (String) The fully qualified name of the stage containing the service specification file. At symbol (@) is added automatically. Example: "\"<db_name>\".\"<schema_name>\".\"<stage_name>\"". For more information about this resource, see docs.
                            The fully qualified name of the stage containing the service specification template file. At symbol (`@`) is added automatically. Example: `"\"<db_name>\".\"<schema_name>\".\"<stage_name>\""`. For more information about this resource, see [docs](./stage).
                          type: string
                        text:
                          description: |-
                            (String) The embedded text of the service specification.
                            The embedded text of the service specification template.
                          type: string
                        using:
                          description: |-
                            (Block List, Min: 1) List of the specified template variables and the values of those variables. (see below for nested schema)
                            List of the specified template variables and the values of those variables.
                          items:
                            properties:
                              key:
                                description: |-
                                  (String) The name of the template variable. The provider wraps it in double quotes by default, so be aware of that while referencing the argument in the spec definition.
                                  The name of the template variable. The provider wraps it in double quotes by default, so be aware of that while referencing the argument in the spec definition.
                                type: string
                              value:
                                description: |-
                                  (String) The value to assign to the variable in the template. The provider wraps it in $$ by default, so be aware of that while referencing the argument in the spec definition. The value must either be alphanumeric or valid JSON.
                                  The value to assign to the variable in the template. The provider wraps it in `$$` by default, so be aware of that while referencing the argument in the spec definition. The value must either be alphanumeric or valid JSON.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  fullyQualifiedName:
                    description: |-
                      (String) Fully qualified name of the resource. For more information, see object name resolution.
                      Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  maxInstances:
                    description: |-
                      (Number) Specifies the maximum number of service instances to run.
                      Specifies the maximum number of service instances to run.
                    type: number
                  minInstances:
                    description: |-
                      (Number) Specifies the minimum number of service instances to run.
                      Specifies the minimum number of service instances to run.
                    type: number
                  minReadyInstances:
                    description: |-
                      (Number) Indicates the minimum service instances that must be ready for Snowflake to consider the service is ready to process requests.
                      Indicates the minimum service instances that must be ready for Snowflake to consider the service is ready to process requests.
                    type: number
                  queryWarehouse:
                    description: |-
                      (String) Warehouse to use if a service container connects to Snowflake to execute a query but does not explicitly specify a warehouse to use. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      Warehouse to use if a service container connects to Snowflake to execute a query but does not explicitly specify a warehouse to use. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  schema:
                    description: |-
                      (String) The schema in which to create the service. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the service. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  serviceType:
                    description: |-
                      (String) Specifies a type for the service. This field is used for checking external changes and recreating the resources if needed.
                      Specifies a type for the service. This field is used for checking external changes and recreating the resources if needed.
                    type: string
                  showOutput:
                    description: |-
                      (List of Object) Outputs the result of SHOW SERVICES for the given service. (see below for nested schema)
                      Outputs the result of `SHOW SERVICES` for the given service.
                    items:
                      properties:
                        autoResume:
                          description: 'uses special value that cannot be set in the
                            configuration manually (default)) Specifies whether to
                            automatically resume a service. Available options are:
                            "true" or "false". When the value is not set in the configuration
                            the provider will put "default" there which means to use
                            the Snowflake default for this value.'
                          type: boolean
                        autoSuspendSecs:
                          description: uses special value that cannot be set in the
                            configuration manually (-1)) Specifies the number of seconds
                            of inactivity (service is idle) after which Snowflake
                            automatically suspends the service.
                          type: number
                        comment:
                          description: (String) Specifies a comment for the service.
                          type: string
                        computePool:
                          description: 'case characters are not supported. This limitation
                            in the provider follows the limitation in Snowflake (see
                            docs). Due to technical limitations (read more here),
                            avoid using the following characters: |, ., ".'
                          type: string
                        createdOn:
                          description: (String)
                          type: string
                        currentInstances:
                          description: (Number)
                          type: number
                        databaseName:
                          description: (String)
                          type: string
                        dnsName:
                          description: (String)
                          type: string
                        externalAccessIntegrations:
                          description: (Set of String) Specifies the names of the
                            external access integrations that allow your service to
                            access external sites.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        isAsyncJob:
                          description: (Boolean)
                          type: boolean
                        isJob:
                          description: (Boolean)
                          type: boolean
                        isUpgrading:
                          description: (Boolean)
                          type: boolean
                        managingObjectDomain:
                          description: (String)
                          type: string
                        managingObjectName:
                          description: (String)
                          type: string
                        maxInstances:
                          description: (Number) Specifies the maximum number of service
                            instances to run.
                          type: number
                        minInstances:
                          description: (Number) Specifies the minimum number of service
                            instances to run.
                          type: number
                        minReadyInstances:
                          description: (Number) Indicates the minimum service instances
                            that must be ready for Snowflake to consider the service
                            is ready to process requests.
                          type: number
                        name:
                          description: '(String) Specifies the identifier for the
                            service; must be unique for the schema in which the service
                            is created. Due to technical limitations (read more here),
                            avoid using the following characters: |, ., ".'
                          type: string
                        owner:
                          description: (String)
                          type: string
                        ownerRoleType:
                          description: (String)
                          type: string
                        queryWarehouse:
                          description: '(String) Warehouse to use if a service container
                            connects to Snowflake to execute a query but does not
                            explicitly specify a warehouse to use. Due to technical
                            limitations (read more here), avoid using the following
                            characters: |, ., ".'
                          type: string
                        resumedOn:
                          description: (String)
                          type: string
                        schemaName:
                          description: (String)
                          type: string
                        specDigest:
                          description: (String)
                          type: string
                        status:
                          description: (String)
                          type: string
                        suspendedOn:
                          description: (String)
                          type: string
                        targetInstances:
                          description: (Number)
                          type: number
                        updatedOn:
                          description: (String)
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
